<%= simple_form_for(@puppet_machine) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :puppet %>
    <%= f.association :machine %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
