<%= simple_form_for(@key) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= label(:key, :password) %>
    <%= password_field(:key, :password) %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
